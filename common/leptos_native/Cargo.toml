[package]
name = "leptos_native"
version = "0.1.0"
authors = ["Greg Johnston", "Seph Gentle"]
license = "MIT"
description = "Native UI port of Leptos. Renderer-agnostic core."
rust-version.workspace = true
edition.workspace = true

[dependencies]
either_of = { workspace = true }
or_poisoned = { workspace = true }
oco_ref = { workspace = true }
throw_error = { workspace = true }
any_spawner = { workspace = true, features = ["futures-executor"] }
leptos_macro = { workspace = true }
reactive_graph = { workspace = true, features = ["effects"] }
futures = { workspace = true, default-features = true }
send_wrapper = { workspace = true, default-features = true }
paste = { workspace = true, default-features = true }
rustc-hash = { workspace = true, default-features = true }
slotmap = { workspace = true, default-features = true }
tracing = { optional = true, workspace = true, default-features = true }
typed-builder = { workspace = true, default-features = true }
typed-builder-macro = { workspace = true, default-features = true }
thiserror = { workspace = true, default-features = true }

indexmap = { workspace = true, default-features = true }
# Layout module: Taffy-backed storage tree + style setters.
# Folded in (instead of being a separate `native_layout` crate)
# so the `IntoMaybeReactive` trait and the taffy types it impls
# for live in the same crate (orphan rule).
taffy = { workspace = true }

[features]
# Previously we could disable reactive_graph

default = ["reactive_graph"]
nightly = ["leptos_macro/nightly", "reactive_graph/nightly"]
tracing = [
  "dep:tracing",
  "reactive_graph/tracing",
  "leptos_macro/tracing",
]
reactive_graph = [] # Not sure if anything works if this is disabled. TODO.

[build-dependencies]
rustc_version = { workspace = true, default-features = true }

# See note in upstream Cargo.toml — proc macros don't see RUSTFLAGS
# when --target is specified, so the erasure feature is enabled via
# this normal-crate dependency rather than a cfg.
[target.'cfg(erase_components)'.dependencies]
leptos_macro = { workspace = true, features = ["__internal_erase_components"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
  'cfg(leptos_debuginfo)',
  'cfg(rustc_nightly)',
] }
